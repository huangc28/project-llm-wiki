---
quick_id: 260515-od6
status: complete
date: 2026-05-15
---

# Quick Task 260515-od6: Improve project-wiki-init UX to apply by default

## Goal

Make `$project-wiki-init` initialize or update by default, while preserving an explicit dry-run/preview path.

## Plan

1. Update `skills/project-wiki-init/SKILL.md` so the alias runs `project-wiki init` by default and only uses `--dry-run` when requested.
2. Update `README.md` existing-repo guidance to describe apply-by-default behavior and the explicit preview path.
3. Add regression tests that lock the alias and README UX contract.
4. Run targeted and full package tests, then record summary and state.

---
phase: 03-lint-and-safety-checks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/project-llm-wiki/scripts/project_wiki.py
  - skills/project-llm-wiki/tests/test_project_wiki_lint.py
  - skills/project-llm-wiki/tests/test_project_wiki_package.py
autonomous: true
requirements:
  - LINT-01
  - LINT-02
  - LINT-04
  - TEST-04
must_haves:
  truths:
    - "D-01: lint checks Obsidian-style `[[wikilink]]` links only, not general Markdown links."
    - "D-02: wikilink existence checks support `[[path]]`, `[[path.md]]`, `[[path|Alias]]`, and `[[path#Heading]]`."
    - "D-03: alias text and heading fragments are ignored; the linked page must exist, but the heading is not validated."
    - "D-04: broken wikilinks produce `error` findings and make `project-wiki lint` exit 1."
    - "D-05: missing `.llm-wiki/index.md` coverage for main wiki pages produces `warning` findings."
    - "D-06: files under `.llm-wiki/raw/curated/` are excluded from index coverage requirements."
    - "D-07: lint does not require standalone topic pages to contain outgoing links."
    - "D-11: file size warnings apply only under `.llm-wiki/raw/`."
    - "D-12: a `.llm-wiki/raw/` file larger than 102400 bytes produces an `oversized_raw_file` warning."
    - "D-20: error findings exit 1; warning-only findings exit 0."
    - "D-26: lint is read-only and does not modify wiki files."
  artifacts:
    - path: skills/project-llm-wiki/scripts/project_wiki.py
      provides: "real `project-wiki lint` entrypoint, wikilink parser, index coverage check, and raw file size check"
      contains: "def run_lint"
    - path: skills/project-llm-wiki/tests/test_project_wiki_lint.py
      provides: "temporary Git repo subprocess fixtures for structural lint behavior"
      contains: "class ProjectWikiLintTests"
    - path: skills/project-llm-wiki/tests/test_project_wiki_package.py
      provides: "updated stdlib import whitelist for new lint modules"
      contains: "test_helper_imports_only_allowed_modules"
  key_links:
    - from: skills/project-llm-wiki/scripts/project_wiki.py
      to: ".llm-wiki/"
      via: "`resolve_git_root(pathlib.Path.cwd())` then `git_root / WIKI_ROOT`"
      pattern: "resolve_git_root\\(cwd\\)"
    - from: skills/project-llm-wiki/scripts/project_wiki.py
      to: "Obsidian wikilinks"
      via: "`extract_wikilinks` plus `normalize_wikilink_target`"
      pattern: "\\[\\["
    - from: skills/project-llm-wiki/tests/test_project_wiki_lint.py
      to: skills/project-llm-wiki/scripts/project_wiki.py
      via: "subprocess helper execution from temporary Git repositories"
      pattern: "project_wiki.py"
---

<objective>
Implement the first lint slice: command entrypoint, Obsidian wikilink existence checks, index coverage warnings, and raw file size warnings.

Purpose: make the highest-signal structural and raw-size wiki issues visible before rollout while preserving the existing git-root boundary and read-only behavior.
Output: a real `project-wiki lint` command path in `project_wiki.py`, a new lint subprocess fixture file, and import-whitelist updates for used stdlib modules.
</objective>

<execution_context>
@/Users/huangchihan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangchihan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-lint-and-safety-checks/03-CONTEXT.md
@.planning/phases/03-lint-and-safety-checks/03-RESEARCH.md
@.planning/phases/03-lint-and-safety-checks/03-VALIDATION.md
@.planning/phases/03-lint-and-safety-checks/03-PATTERNS.md
@.planning/phases/02-init-and-wiki-templates/02-01-SUMMARY.md
@.planning/phases/02-init-and-wiki-templates/02-02-SUMMARY.md
@skills/project-llm-wiki/scripts/project_wiki.py
@skills/project-llm-wiki/tests/test_project_wiki_init.py
@skills/project-llm-wiki/tests/test_project_wiki_package.py
@skills/project-llm-wiki/assets/templates/llm-wiki/index.md
@skills/project-llm-wiki/assets/templates/llm-wiki/raw/README.md
@skills/project-llm-wiki/assets/templates/llm-wiki/raw/curated/README.md

<interfaces>
Existing command and repo-boundary surface from `skills/project-llm-wiki/scripts/project_wiki.py`:
```python
def resolve_git_root(cwd: pathlib.Path) -> tuple[pathlib.Path | None, str | None]
def path_is_under(path: pathlib.Path, root: pathlib.Path) -> bool
def print_text_section(heading: str, items: list[str]) -> None
def build_parser()
def main(argv: list[str] | None = None) -> int
```

New contracts this plan must create for later Phase 03 plans:
```python
def run_lint(args) -> int
def make_finding(severity: str, code: str, path: pathlib.Path | str, message: str, remediation: str) -> dict[str, str]
def collect_lint_files(git_root: pathlib.Path) -> list[pathlib.Path]
def read_wiki_text(path: pathlib.Path, git_root: pathlib.Path) -> tuple[str | None, dict[str, str] | None]
def extract_wikilinks(markdown_text: str) -> list[str]
def normalize_wikilink_target(raw_target: str) -> str
def check_broken_wikilinks(git_root: pathlib.Path, markdown_files: list[pathlib.Path]) -> list[dict[str, str]]
def check_index_coverage(git_root: pathlib.Path, markdown_files: list[pathlib.Path]) -> list[dict[str, str]]
def check_raw_file_sizes(git_root: pathlib.Path) -> list[dict[str, str]]
```

Required finding codes from this plan: `broken_wikilink`, `missing_index_entry`, and `oversized_raw_file`.

Continue the existing subprocess fixture pattern from `test_project_wiki_init.py`:
```python
subprocess.run([sys.executable, str(HELPER), *args], cwd=cwd, capture_output=True, text=True, check=False)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace planned lint stub with read-only wikilink lint</name>
  <files>skills/project-llm-wiki/scripts/project_wiki.py, skills/project-llm-wiki/tests/test_project_wiki_lint.py, skills/project-llm-wiki/tests/test_project_wiki_package.py</files>
  <read_first>
    - skills/project-llm-wiki/scripts/project_wiki.py
    - skills/project-llm-wiki/tests/test_project_wiki_lint.py
    - skills/project-llm-wiki/tests/test_project_wiki_package.py
    - skills/project-llm-wiki/tests/test_project_wiki_init.py
    - skills/project-llm-wiki/assets/templates/llm-wiki/index.md
    - .planning/phases/03-lint-and-safety-checks/03-CONTEXT.md
    - .planning/phases/03-lint-and-safety-checks/03-RESEARCH.md
    - .planning/phases/03-lint-and-safety-checks/03-VALIDATION.md
    - .planning/phases/03-lint-and-safety-checks/03-PATTERNS.md
  </read_first>
  <behavior>
    - Clean `.llm-wiki/` generated by `project-wiki init` exits 0 and prints `No issues found in .llm-wiki/`.
    - `[[missing-page]]` in a wiki Markdown file reports `broken_wikilink` with `severity` exactly `error`.
    - `[[features/ideas|Ideas]]`, `[[features/ideas#Related links]]`, and `[[features/ideas.md]]` resolve to `.llm-wiki/features/ideas.md`.
    - The command does not inspect general Markdown links such as `[label](missing.md)`.
  </behavior>
  <action>
    Create `skills/project-llm-wiki/tests/test_project_wiki_lint.py` using only stdlib imports. Use the same constants and helpers as `test_project_wiki_init.py`: `ROOT`, `PACKAGE`, `HELPER`, `run_git`, `init_repo`, and `run_helper`.

    Add tests named:
    - `test_lint_clean_initialized_wiki_reports_success`
    - `test_lint_reports_broken_obsidian_wikilink_as_error`
    - `test_lint_supports_alias_heading_and_md_wikilink_forms`
    - `test_lint_ignores_general_markdown_links`

    In `project_wiki.py`, replace the `lint` planned handler with `run_lint`. Add `lint.add_argument("--json", action="store_true", help="render lint findings as JSON")` now so the command surface is stable for later output tests.

    Add `import json` and `import re` only if this task uses them. Update `test_project_wiki_package.py` allowed imports to include exactly the new stdlib imports used by `project_wiki.py`; do not add `datetime` until stale checks are implemented.

    Implement `make_finding` returning a dictionary with exactly the keys `severity`, `code`, `path`, `message`, and `remediation`. All paths in findings must be repo-relative POSIX strings such as `.llm-wiki/features/ideas.md`.

    Implement `run_lint(args)` to:
    - Resolve `pathlib.Path.cwd()` through `resolve_git_root`.
    - If no git root exists, print `No git repository found for current directory.` and return 2.
    - Require `git_root / ".llm-wiki"` to exist as a real directory under the git root; missing wiki root returns 2 with `No .llm-wiki directory found in the resolved Git root.`
    - Collect Markdown files under `.llm-wiki/` in sorted POSIX order without following symlink escapes.
    - Run `check_broken_wikilinks` and render findings.
    - Print `No issues found in .llm-wiki/` when no findings exist.
    - Return 1 when any finding has `severity == "error"` and 0 otherwise.

    Implement `extract_wikilinks` with a targeted `[[...]]` regex. Implement `normalize_wikilink_target` to strip alias text after `|`, strip heading fragments after `#`, append `.md` when no suffix is present, and reject empty normalized targets. Do not parse or validate Markdown `[label](url)` links.

    Implement page existence resolution relative to `.llm-wiki/`. A link to `features/ideas`, `features/ideas.md`, `features/ideas|Alias`, or `features/ideas#Heading` must resolve to `.llm-wiki/features/ideas.md`. Do not validate heading existence.
  </action>
  <verify>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_lint.py"</automated>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_package.py"</automated>
    <automated>python3 skills/project-llm-wiki/scripts/project_wiki.py lint --help</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def run_lint|def extract_wikilinks|def normalize_wikilink_target|def check_broken_wikilinks|broken_wikilink" skills/project-llm-wiki/scripts/project_wiki.py` finds all required names.
    - `rg -n "lint.add_argument\\(.*--json|--json" skills/project-llm-wiki/scripts/project_wiki.py` finds the JSON flag.
    - `rg -n "test_lint_clean_initialized_wiki_reports_success|test_lint_reports_broken_obsidian_wikilink_as_error|test_lint_supports_alias_heading_and_md_wikilink_forms|test_lint_ignores_general_markdown_links" skills/project-llm-wiki/tests/test_project_wiki_lint.py` finds all four tests.
    - `rg -n "No issues found in \\.llm-wiki/|No \\.llm-wiki directory found in the resolved Git root\\." skills/project-llm-wiki/scripts/project_wiki.py` finds both output phrases.
    - `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_lint.py"` exits 0.
  </acceptance_criteria>
  <done>`project-wiki lint` is a real read-only command and reports broken Obsidian wikilinks as `error` findings with the fixed finding fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add index coverage and raw file size checks</name>
  <files>skills/project-llm-wiki/scripts/project_wiki.py, skills/project-llm-wiki/tests/test_project_wiki_lint.py</files>
  <read_first>
    - skills/project-llm-wiki/scripts/project_wiki.py
    - skills/project-llm-wiki/tests/test_project_wiki_lint.py
    - skills/project-llm-wiki/assets/templates/llm-wiki/index.md
    - skills/project-llm-wiki/assets/templates/llm-wiki/raw/README.md
    - skills/project-llm-wiki/assets/templates/llm-wiki/raw/curated/README.md
    - .planning/phases/03-lint-and-safety-checks/03-CONTEXT.md
    - .planning/phases/03-lint-and-safety-checks/03-RESEARCH.md
    - .planning/phases/03-lint-and-safety-checks/03-VALIDATION.md
    - .planning/phases/03-lint-and-safety-checks/03-PATTERNS.md
  </read_first>
  <behavior>
    - Main wiki pages missing from `.llm-wiki/index.md` report `missing_index_entry` warnings and lint exits 0 when no errors exist.
    - Files under `.llm-wiki/raw/curated/` are not required to appear in `index.md`.
    - Lint does not require every page to have outgoing wikilinks.
    - Raw files larger than 102400 bytes report `oversized_raw_file` warnings, and non-raw files are not size-checked.
  </behavior>
  <action>
    Add tests named:
    - `test_lint_reports_missing_index_entry_as_warning`
    - `test_lint_does_not_require_raw_curated_sources_in_index`
    - `test_lint_does_not_require_outgoing_links_on_topic_pages`
    - `test_lint_reports_oversized_raw_file_warning`
    - `test_lint_does_not_size_check_non_raw_pages`

    Implement `check_index_coverage(git_root, markdown_files)` with these exact rules:
    - Read `.llm-wiki/index.md` as UTF-8 and extract normalized Obsidian wikilinks from it.
    - Candidate main wiki pages are Markdown files under `.llm-wiki/architecture/`, `.llm-wiki/domain/`, `.llm-wiki/decisions/`, `.llm-wiki/operations/`, `.llm-wiki/features/`, `.llm-wiki/summaries/`, `.llm-wiki/raw/README.md`, and `.llm-wiki/raw/curated/README.md`.
    - Exclude `.llm-wiki/README.md`, `.llm-wiki/AGENTS.md`, `.llm-wiki/index.md`, `.llm-wiki/log.md`, `.llm-wiki/raw/curated/*`, and any `.gitkeep`.
    - For each candidate page not linked from `index.md`, emit `make_finding("warning", "missing_index_entry", relative_path, ..., ...)`.
    - Use remediation text that tells the user to add a `[[relative/path-without-md]]` link to `.llm-wiki/index.md` when the page is durable main wiki knowledge.

    Implement `check_raw_file_sizes(git_root)` with a constant named `RAW_SIZE_WARNING_BYTES = 100 * 1024`. It must inspect files under `.llm-wiki/raw/` only, including nested files, and emit `oversized_raw_file` warnings for files larger than 102400 bytes. Do not apply this check to `.llm-wiki/features/`, `.llm-wiki/summaries/`, or other main wiki directories.

    Update `run_lint` to call `check_broken_wikilinks`, `check_index_coverage`, and `check_raw_file_sizes` in that deterministic order. Sort findings by `(severity != "error", path, code, message)` or another stable key that keeps output deterministic.
  </action>
  <verify>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_lint.py"</automated>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def check_index_coverage|missing_index_entry|def check_raw_file_sizes|RAW_SIZE_WARNING_BYTES|oversized_raw_file" skills/project-llm-wiki/scripts/project_wiki.py` finds all required names.
    - `rg -n "raw/curated|features|summaries|architecture|domain|decisions|operations" skills/project-llm-wiki/scripts/project_wiki.py` finds the index coverage categories and raw-curated exclusion logic.
    - `rg -n "test_lint_reports_missing_index_entry_as_warning|test_lint_does_not_require_raw_curated_sources_in_index|test_lint_does_not_require_outgoing_links_on_topic_pages|test_lint_reports_oversized_raw_file_warning|test_lint_does_not_size_check_non_raw_pages" skills/project-llm-wiki/tests/test_project_wiki_lint.py` finds all five tests.
    - `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_lint.py"` exits 0.
    - `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests` exits 0.
  </acceptance_criteria>
  <done>Lint reports missing index entries and oversized raw files as warning-only findings without adding outgoing-link requirements or raw-curated index requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user cwd -> Git root resolver | The current directory determines which repository's `.llm-wiki/` is inspected. |
| `.llm-wiki/` files -> lint parser | Markdown content may contain malformed links, invalid UTF-8, or path-like strings. |
| `.llm-wiki/raw/` filesystem -> raw-size check | Raw files may be large dumps or symlink-like structures that must not escape the repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `check_broken_wikilinks` | mitigate | Normalize only Obsidian `[[...]]` destinations and verify resolved targets under `.llm-wiki/`; broken links produce `error`. |
| T-03-02 | Information Disclosure | `collect_lint_files` | mitigate | Inspect only files under the resolved Git root and `.llm-wiki/`; reuse `path_is_under` and avoid symlink escapes. |
| T-03-03 | Denial of Service | `check_raw_file_sizes` | mitigate | Use byte-size metadata for raw size warnings; do not read large raw files just to determine size. |
| T-03-04 | Tampering | `run_lint` exit code | mitigate | Return 1 only for `error` findings; warning-only structural debt remains visible but non-blocking. |
| T-03-05 | Repudiation | finding fields | mitigate | Emit fixed fields `severity`, `code`, `path`, `message`, and `remediation` for every finding. |
</threat_model>

<verification>
Run `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_lint.py"` after each task, then run the full package suite after Task 2. Spot-check `python3 skills/project-llm-wiki/scripts/project_wiki.py lint --help` to confirm `--json` is exposed.
</verification>

<success_criteria>
- `project-wiki lint` replaces the planned lint stub and runs against `.llm-wiki/` in the resolved git root.
- Broken Obsidian wikilinks produce `broken_wikilink` error findings and exit 1.
- Missing main wiki pages in `index.md` produce `missing_index_entry` warnings and warning-only exit 0.
- Raw files larger than 102400 bytes produce `oversized_raw_file` warnings, with raw-size checks limited to `.llm-wiki/raw/`.
- New lint tests and package import-whitelist tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-lint-and-safety-checks/03-01-SUMMARY.md`.
</output>

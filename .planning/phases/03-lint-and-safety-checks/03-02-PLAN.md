---
phase: 03-lint-and-safety-checks
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - skills/project-llm-wiki/scripts/project_wiki.py
  - skills/project-llm-wiki/tests/test_project_wiki_lint.py
  - skills/project-llm-wiki/tests/test_project_wiki_package.py
autonomous: true
requirements:
  - LINT-03
  - LINT-05
  - LINT-06
  - TEST-05
must_haves:
  truths:
    - "D-08: secret-looking detection uses high-confidence patterns only."
    - "D-09: secret-looking detection scans all regular files returned by `collect_wiki_files(git_root)` across the entire `.llm-wiki/` tree, not only Markdown files."
    - "D-10: secret-looking findings are `warning` findings and do not fail lint by themselves."
    - "D-13: stale checks use only top-of-file page frontmatter `updated:`."
    - "D-14: pages without `updated:` are not stale-checked."
    - "D-15: an `updated:` date older than 90 days produces a `stale_page` warning."
    - "D-16: stale findings tell the user to review against current repo files; lint never updates `updated:` automatically."
    - "D-17: repo/wiki contradiction warnings are limited to conservative repo path drift."
    - "D-18: repo path drift checks inspect only Markdown inline code spans and fenced code blocks."
    - "D-19: missing repo path references produce `warning` findings, not errors."
    - "D-20: warning-only secret, stale, size, and path-drift findings exit 0."
    - "D-26: lint is read-only across all warning-producing checks."
  artifacts:
    - path: skills/project-llm-wiki/scripts/project_wiki.py
      provides: "secret-like, stale frontmatter, and repo path drift lint checks"
      contains: "secret_like_content"
    - path: skills/project-llm-wiki/tests/test_project_wiki_lint.py
      provides: "subprocess fixtures for warning-only safety and drift behavior"
      contains: "test_lint_reports_secret_like_non_markdown_raw_file_warning"
    - path: skills/project-llm-wiki/tests/test_project_wiki_package.py
      provides: "updated import whitelist for `datetime` when stale checks use it"
      contains: "datetime"
  key_links:
    - from: skills/project-llm-wiki/scripts/project_wiki.py
      to: "all regular `.llm-wiki/` files"
      via: "`check_secret_like_content` receives files from `collect_wiki_files(git_root)`, while stale and path-drift checks use `collect_markdown_files(git_root)`"
      pattern: "check_secret_like_content\\(git_root, wiki_files\\)"
    - from: skills/project-llm-wiki/scripts/project_wiki.py
      to: "current repository filesystem"
      via: "`check_repo_path_drift` path existence checks rooted at git root"
      pattern: "missing_repo_path"
    - from: skills/project-llm-wiki/tests/test_project_wiki_lint.py
      to: "warning-only exit behavior"
      via: "subprocess return-code assertions"
      pattern: "returncode"
---

<objective>
Add the second lint slice: high-confidence secret-looking warnings, stale page warnings, and deterministic repo path drift warnings.

Purpose: make unsafe, stale, and repo/wiki drift signals visible without introducing noisy semantic analysis, automatic edits, or warning-only command failures.
Output: additional lint helpers in `project_wiki.py`, expanded subprocess fixtures, and any required stdlib import whitelist updates.
</objective>

<execution_context>
@/Users/huangchihan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangchihan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-lint-and-safety-checks/03-CONTEXT.md
@.planning/phases/03-lint-and-safety-checks/03-RESEARCH.md
@.planning/phases/03-lint-and-safety-checks/03-VALIDATION.md
@.planning/phases/03-lint-and-safety-checks/03-PATTERNS.md
@.planning/phases/03-lint-and-safety-checks/03-01-SUMMARY.md
@skills/project-llm-wiki/scripts/project_wiki.py
@skills/project-llm-wiki/tests/test_project_wiki_lint.py
@skills/project-llm-wiki/tests/test_project_wiki_package.py
@skills/project-llm-wiki/assets/templates/llm-wiki/raw/README.md

<interfaces>
Use the finding and lint pipeline contracts created by Plan 03-01:
```python
def make_finding(severity: str, code: str, path: pathlib.Path | str, message: str, remediation: str) -> dict[str, str]
def collect_wiki_files(git_root: pathlib.Path) -> list[pathlib.Path]        # all regular files under .llm-wiki/ for all-wiki checks
def collect_markdown_files(git_root: pathlib.Path) -> list[pathlib.Path]    # Markdown files only for Markdown-specific checks
def collect_lint_files(git_root: pathlib.Path) -> list[pathlib.Path]        # optional alias for collect_markdown_files only; never use for secret scanning
def read_wiki_text(path: pathlib.Path, git_root: pathlib.Path) -> tuple[str | None, dict[str, str] | None]
def run_lint(args) -> int
```

New contracts this plan must add:
```python
def check_secret_like_content(git_root: pathlib.Path, wiki_files: list[pathlib.Path]) -> list[dict[str, str]]
def parse_updated_frontmatter(markdown_text: str) -> datetime.date | None
def check_stale_pages(git_root: pathlib.Path, markdown_files: list[pathlib.Path]) -> list[dict[str, str]]
def extract_markdown_code_references(markdown_text: str) -> list[str]
def check_repo_path_drift(git_root: pathlib.Path, markdown_files: list[pathlib.Path]) -> list[dict[str, str]]
```

Required finding codes from this plan: `secret_like_content`, `stale_page`, and `missing_repo_path`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add secret-looking and stale page warnings</name>
  <files>skills/project-llm-wiki/scripts/project_wiki.py, skills/project-llm-wiki/tests/test_project_wiki_lint.py, skills/project-llm-wiki/tests/test_project_wiki_package.py</files>
  <read_first>
    - skills/project-llm-wiki/scripts/project_wiki.py
    - skills/project-llm-wiki/tests/test_project_wiki_lint.py
    - skills/project-llm-wiki/tests/test_project_wiki_package.py
    - skills/project-llm-wiki/assets/templates/llm-wiki/raw/README.md
    - .planning/phases/03-lint-and-safety-checks/03-CONTEXT.md
    - .planning/phases/03-lint-and-safety-checks/03-RESEARCH.md
    - .planning/phases/03-lint-and-safety-checks/03-VALIDATION.md
    - .planning/phases/03-lint-and-safety-checks/03-PATTERNS.md
  </read_first>
  <behavior>
    - A high-confidence private key block or credential-bearing URL in any regular file under `.llm-wiki/`, including a non-`.md` raw file, reports `secret_like_content` with severity `warning`.
    - The default raw policy prose does not produce a secret-looking warning by itself.
    - Provider-specific token prefixes are not scanned in Phase 3 unless later tests add false-positive coverage.
    - Secret scanning does not follow symlink escapes outside `.llm-wiki/` or the resolved Git root.
    - Unreadable wiki files inside an existing `.llm-wiki/` produce `error` findings with path and remediation so text and JSON output can report them.
    - Frontmatter `updated: 2000-01-01` reports `stale_page` with severity `warning`.
    - A page without frontmatter `updated:` produces no stale finding.
    - Stale and secret-like warnings do not modify files and exit 0 when no errors exist.
  </behavior>
  <action>
    Add tests named:
    - `test_lint_reports_secret_like_raw_content_warning`
    - `test_lint_reports_secret_like_non_markdown_raw_file_warning`
    - `test_lint_scans_secret_like_content_outside_raw`
    - `test_lint_default_raw_policy_does_not_trigger_secret_warning`
    - `test_lint_secret_scan_does_not_follow_symlink_escape`
    - `test_lint_reports_unreadable_wiki_file_for_all_file_scan`
    - `test_lint_reports_stale_updated_frontmatter_warning`
    - `test_lint_ignores_pages_without_updated_frontmatter`
    - `test_warning_only_secret_and_stale_findings_exit_zero`

    Add `import datetime` to `project_wiki.py` only when implementing stale checks. Update `test_project_wiki_package.py` allowed imports to include `datetime` only if the helper imports it.

    Implement `check_secret_like_content(git_root, wiki_files)` using high-confidence patterns only. It must receive the all-file inventory from `collect_wiki_files(git_root)`, not `collect_markdown_files(git_root)` or the Markdown-only `collect_lint_files` alias. Include at least these categories:
    - Private key block delimiters matching `-----BEGIN ... PRIVATE KEY-----`.
    - Credential-bearing URLs matching a scheme followed by `://user:password@host` where both user and password portions are nonempty and contain no whitespace.
    Do not match on keywords such as `secret`, `credential`, `token`, or `password` by themselves, because raw policy documentation contains those words. Do not add provider-specific token prefixes in Phase 3 unless tests first cover false-positive behavior.

    `check_secret_like_content` must scan all files returned by `collect_wiki_files`, not only Markdown files and not only `.llm-wiki/raw/`. Add a TEST-05 fixture that writes a non-`.md` raw file such as `.llm-wiki/raw/curated/unsafe.env` containing either a private key delimiter or a credential-bearing URL, then asserts `secret_like_content`, `warning`, the non-`.md` path, and remediation containing `redact`. It must emit `make_finding("warning", "secret_like_content", relative_path, ..., ...)` with remediation that tells the user to remove or redact the unsafe material before committing the wiki.

    If an all-file secret scan cannot read a wiki file inside an existing `.llm-wiki/`, convert the read failure into `make_finding("error", "unreadable_wiki_file", relative_path, ..., ...)` with remediation instead of returning operational exit 2. Missing Git repo and missing `.llm-wiki/` remain operational exit 2.

    Implement `parse_updated_frontmatter` to inspect only top-of-file YAML-style frontmatter between starting and ending `---` lines. It should return `datetime.date.fromisoformat(value)` only for a simple `updated: YYYY-MM-DD` line. Invalid dates should produce no stale finding unless `read_wiki_text` already produced an unreadable-file finding.

    Implement `check_stale_pages` with `STALE_AFTER_DAYS = 90`. For `updated:` older than 90 days, emit `stale_page` warning with remediation that says to review the page against current repo files and update `updated:` only after validation. Do not write the updated date.

    Update `run_lint` to call `check_secret_like_content(git_root, wiki_files)` and `check_stale_pages(git_root, markdown_files)` after the Plan 03-01 checks. Keep warning-only runs exiting 0.
  </action>
  <verify>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_lint.py"</automated>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_package.py"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def check_secret_like_content|collect_wiki_files\\(git_root\\)|secret_like_content|PRIVATE KEY|://.*:.*@|unreadable_wiki_file|def parse_updated_frontmatter|def check_stale_pages|STALE_AFTER_DAYS|stale_page" skills/project-llm-wiki/scripts/project_wiki.py` finds all required names and patterns.
    - `rg -n "test_lint_reports_secret_like_raw_content_warning|test_lint_reports_secret_like_non_markdown_raw_file_warning|test_lint_scans_secret_like_content_outside_raw|test_lint_default_raw_policy_does_not_trigger_secret_warning|test_lint_secret_scan_does_not_follow_symlink_escape|test_lint_reports_unreadable_wiki_file_for_all_file_scan|test_lint_reports_stale_updated_frontmatter_warning|test_lint_ignores_pages_without_updated_frontmatter|test_warning_only_secret_and_stale_findings_exit_zero" skills/project-llm-wiki/tests/test_project_wiki_lint.py` finds all nine tests.
    - `rg -n "unsafe\\.env|raw/curated/unsafe|BEGIN .*PRIVATE KEY|://[^[:space:]]+:[^[:space:]]+@" skills/project-llm-wiki/tests/test_project_wiki_lint.py` finds the TEST-05 non-Markdown raw secret fixture.
    - `rg -n "datetime" skills/project-llm-wiki/tests/test_project_wiki_package.py` finds the import whitelist update if `project_wiki.py` imports `datetime`.
    - `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_lint.py"` exits 0.
  </acceptance_criteria>
  <done>Secret-looking content across all regular `.llm-wiki/` files, including non-Markdown raw files, and stale `updated:` frontmatter produce read-only lint findings with high-confidence matching, no policy-prose false positives, and unreadable-file errors surfaced as findings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add conservative repo path drift warnings</name>
  <files>skills/project-llm-wiki/scripts/project_wiki.py, skills/project-llm-wiki/tests/test_project_wiki_lint.py</files>
  <read_first>
    - skills/project-llm-wiki/scripts/project_wiki.py
    - skills/project-llm-wiki/tests/test_project_wiki_lint.py
    - .planning/phases/03-lint-and-safety-checks/03-CONTEXT.md
    - .planning/phases/03-lint-and-safety-checks/03-RESEARCH.md
    - .planning/phases/03-lint-and-safety-checks/03-VALIDATION.md
    - .planning/phases/03-lint-and-safety-checks/03-PATTERNS.md
  </read_first>
  <behavior>
    - A missing repo path inside an inline code span reports `missing_repo_path` with severity `warning`.
    - A missing repo path inside a fenced code block reports `missing_repo_path` with severity `warning`.
    - Ordinary prose containing path-like text is ignored by the path drift check.
    - Outside-repo candidates such as `../outside.txt`, `/etc/passwd`, and `~/secret.txt` are ignored or rejected without probing outside the resolved Git root.
    - Ordinary prose mentioning outside-repo paths such as `/tmp/example.txt` or `../outside.txt` is ignored.
    - Existing repo paths inside code spans or fenced blocks do not warn.
    - Missing path warnings do not fail lint by themselves.
  </behavior>
  <action>
    Add tests named:
    - `test_lint_reports_missing_repo_path_from_inline_code_span`
    - `test_lint_reports_missing_repo_path_from_fenced_code_block`
    - `test_lint_ignores_repo_path_like_text_in_plain_prose`
    - `test_lint_ignores_outside_repo_path_drift_candidates`
    - `test_lint_ignores_outside_repo_paths_in_plain_prose`
    - `test_lint_does_not_warn_for_existing_repo_path_reference`
    - `test_warning_only_repo_path_drift_exits_zero`

    Implement `extract_markdown_code_references(markdown_text)` so it extracts only:
    - Inline code span contents from single-backtick spans.
    - Fenced code block contents between triple-backtick fences.
    It must not scan ordinary Markdown prose.

    Implement a conservative repo path candidate filter. A candidate should be considered a repo path only when it is relative, contains `/`, has no whitespace, does not start with `.llm-wiki/`, does not start with `../`, does not start with `/` or `~`, does not start with `http://` or `https://`, and does not contain shell interpolation characters. Prefer path-like strings such as `skills/project-llm-wiki/scripts/project_wiki.py`, `src/app.py`, or `docs/guide.md`.

    Implement `check_repo_path_drift` to check each candidate against `git_root / candidate` only after confirming the resolved candidate stays under `git_root`. If the path does not exist, emit `make_finding("warning", "missing_repo_path", wiki_relative_path, ..., ...)`. The remediation must say to verify whether the wiki reference is stale, update the wiki after checking current repo files, or remove the reference if it is only an example.

    Update `run_lint` to call `check_repo_path_drift` after stale checks. Keep warning-only runs exiting 0 and keep lint read-only.
  </action>
  <verify>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_lint.py"</automated>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def extract_markdown_code_references|def check_repo_path_drift|missing_repo_path" skills/project-llm-wiki/scripts/project_wiki.py` finds all required names.
    - `rg -n "test_lint_reports_missing_repo_path_from_inline_code_span|test_lint_reports_missing_repo_path_from_fenced_code_block|test_lint_ignores_repo_path_like_text_in_plain_prose|test_lint_ignores_outside_repo_path_drift_candidates|test_lint_ignores_outside_repo_paths_in_plain_prose|test_lint_does_not_warn_for_existing_repo_path_reference|test_warning_only_repo_path_drift_exits_zero" skills/project-llm-wiki/tests/test_project_wiki_lint.py` finds all seven tests.
    - `rg -n "\\.\\.\\/outside|/etc/passwd|~/secret|path_is_under|resolve\\(strict=False\\)" skills/project-llm-wiki/scripts/project_wiki.py skills/project-llm-wiki/tests/test_project_wiki_lint.py` finds outside-repo path boundary coverage.
    - `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_lint.py"` exits 0.
    - `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests` exits 0.
  </acceptance_criteria>
  <done>Repo path drift warnings are limited to code spans and fenced blocks, report missing paths as warning-only findings, and ignore ordinary prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wiki text -> secret heuristics | Markdown content may include real secrets or policy prose that mentions unsafe-material categories. |
| wiki frontmatter -> date parser | User-authored frontmatter may contain missing, malformed, or stale `updated:` values. |
| wiki code spans/blocks -> repo path checker | Wiki references to repo paths are untrusted and may point outside the current repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | `check_secret_like_content` | mitigate | Scan the all-file `collect_wiki_files(git_root)` inventory, including non-Markdown raw files; match only high-confidence private key block and credential URL shapes; warn users to redact unsafe material. |
| T-03-07 | Denial of Service | secret scanner | mitigate | Avoid broad keyword matching that would warn on default raw policy prose and create noisy warning debt. |
| T-03-08 | Tampering | `check_stale_pages` | mitigate | Warn on stale `updated:` frontmatter but never edit dates automatically. |
| T-03-09 | Tampering | `check_repo_path_drift` | mitigate | Limit drift warnings to missing repo paths from code spans and fenced blocks; current repo files remain authoritative. |
| T-03-10 | Information Disclosure | path drift candidate handling | mitigate | Check only relative path candidates that resolve under the git root; ignore URLs, `.llm-wiki/` internal paths, parent-directory escapes, absolute paths, home-directory paths, and ordinary prose outside-repo paths. |
</threat_model>

<verification>
Run the targeted lint test suite after each task, then run the full package suite after Task 2. Confirm warning-only scenarios return 0, default template policy prose does not trigger secret-looking warnings, secret scanning catches a non-Markdown raw file, symlink escapes are not followed, and repo path drift ignores outside-repo candidates plus ordinary prose paths.
</verification>

<success_criteria>
- `project-wiki lint` warns on high-confidence secret-looking content in every regular file returned by `collect_wiki_files(git_root)`, including non-Markdown raw files.
- Secret scanning does not use Markdown-only inventories and does not follow symlink escapes outside `.llm-wiki/`.
- `updated:` frontmatter older than 90 days warns, while pages without `updated:` are ignored.
- Repo path drift warnings are emitted only for missing in-repo relative paths found in inline code spans or fenced code blocks; outside-repo candidates and ordinary prose paths are ignored.
- Warning-only secret, stale, and path-drift runs exit 0 and leave wiki files unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/03-lint-and-safety-checks/03-02-SUMMARY.md`.
</output>

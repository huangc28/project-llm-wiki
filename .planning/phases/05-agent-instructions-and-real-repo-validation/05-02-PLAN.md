---
phase: 05-agent-instructions-and-real-repo-validation
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - .planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md
autonomous: true
requirements:
  - TEST-07
must_haves:
  truths:
    - "D-18: `peasydeal_be` validation is dry-run only; Phase 5 must not write to or commit the target repo."
    - "D-19: the dry-run report includes resolved git root, would-create/would-update paths, the managed root `AGENTS.md` section, and conflict status."
    - "TEST-07: the pattern is validated against `peasydeal_be` before any other PeasyDeal repo application."
  artifacts:
    - path: .planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md
      provides: "`peasydeal_be` dry-run evidence and target no-write proof"
      contains: "peasydeal_be Dry-Run Evidence"
  key_links:
    - from: skills/project-llm-wiki/scripts/project_wiki.py
      to: "/Users/huangchihan/develop/bbj/peasydeal/peasydeal_be"
      via: "dry-run command executed from the target repo"
      pattern: "project_wiki.py init --dry-run"
    - from: .planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md
      to: "D-18"
      via: "target git status before and after dry-run are recorded"
      pattern: "Target git status after"
---

<objective>
Validate Phase 5 behavior against the real `peasydeal_be` repository in dry-run mode only.

Purpose: prove the complete init pattern reports the expected `.llm-wiki/` and root `AGENTS.md` changes for the first real target without mutating that repository.
Output: phase-local rollout report section with target path, command, exit code, dry-run output summary, managed section, conflict status, and before/after target git status.
</objective>

<execution_context>
@/Users/huangchihan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangchihan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-agent-instructions-and-real-repo-validation/05-CONTEXT.md
@.planning/phases/05-agent-instructions-and-real-repo-validation/05-RESEARCH.md
@.planning/phases/05-agent-instructions-and-real-repo-validation/05-VALIDATION.md
@.planning/phases/05-agent-instructions-and-real-repo-validation/05-01-SUMMARY.md
@skills/project-llm-wiki/scripts/project_wiki.py

<interfaces>
Plan 05-01 must provide a working dry-run command:
```bash
python3 /Users/huangchihan/develop/bbj/project-llm-wiki/skills/project-llm-wiki/scripts/project_wiki.py init --dry-run
```

Real target repo:
```text
/Users/huangchihan/develop/bbj/peasydeal/peasydeal_be
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Capture peasydeal_be dry-run evidence without writes</name>
  <files>.planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md</files>
  <read_first>
    - .planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md
    - .planning/phases/05-agent-instructions-and-real-repo-validation/05-CONTEXT.md
    - .planning/phases/05-agent-instructions-and-real-repo-validation/05-RESEARCH.md
    - .planning/phases/05-agent-instructions-and-real-repo-validation/05-VALIDATION.md
    - .planning/phases/05-agent-instructions-and-real-repo-validation/05-01-SUMMARY.md
    - skills/project-llm-wiki/scripts/project_wiki.py
  </read_first>
  <behavior>
    - The target repo path is exactly `/Users/huangchihan/develop/bbj/peasydeal/peasydeal_be`.
    - Validation runs `project_wiki.py init --dry-run` from inside `peasydeal_be` only.
    - No files are written to `peasydeal_be`; target `git status --short` before and after is recorded and identical per D-18.
    - Dry-run output includes resolved git root, would-create/would-skip paths, root `AGENTS.md` action, `Managed AGENTS.md section:`, and conflict status per D-19.
  </behavior>
  <action>
    Run these commands and capture the relevant stdout/stderr in the report:
    ```bash
    git -C /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be rev-parse --show-toplevel
    git -C /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be status --short
    python3 /Users/huangchihan/develop/bbj/project-llm-wiki/skills/project-llm-wiki/scripts/project_wiki.py init --dry-run
    git -C /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be status --short
    test ! -d /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be/.llm-wiki
    ```

    The `python3 ... init --dry-run` command must be executed with working directory `/Users/huangchihan/develop/bbj/peasydeal/peasydeal_be`.

    Create `.planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md` with frontmatter:
    ```yaml
    ---
    phase: 05-agent-instructions-and-real-repo-validation
    report: rollout
    target_repo: /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be
    dry_run_only: true
    verdict: PENDING
    ---
    ```

    Add a `## peasydeal_be Dry-Run Evidence` section containing:
    - `Target repo: /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be`
    - `Resolved git root: /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be`
    - `Command: python3 /Users/huangchihan/develop/bbj/project-llm-wiki/skills/project-llm-wiki/scripts/project_wiki.py init --dry-run`
    - `Exit code: 0` if dry-run succeeds; otherwise record the nonzero code and set `Conflict status:` to `BLOCK`.
    - `Target git status before:` with `(clean)` when output is empty, otherwise the exact status lines.
    - `Target git status after:` with `(clean)` when output is empty, otherwise the exact status lines.
    - `Would-create / would-update paths:` copied or summarized from dry-run `Would create paths:` and root `AGENTS.md` action lines.
    - `Managed AGENTS.md section:` followed by the exact section printed by dry-run.
    - `Conflict status: none` when dry-run exits 0 and output has no `Conflicts:` section; otherwise copy the conflict lines.

    Do not run plain `project-wiki init` in `peasydeal_be`. Do not commit, add, restore, delete, or edit any file in `peasydeal_be`.
  </action>
  <verify>
    <automated>git -C /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be status --short</automated>
    <automated>test ! -d /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be/.llm-wiki</automated>
    <automated>rg -n "peasydeal_be Dry-Run Evidence|Resolved git root: /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be|Managed AGENTS.md section:|Conflict status:" .planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be status --short` returns the same output recorded under `Target git status before:` and `Target git status after:`.
    - `test ! -d /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be/.llm-wiki` exits 0.
    - `rg -n "Target repo: /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be|Resolved git root: /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be|dry-run|Managed AGENTS.md section:|Conflict status:" .planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md` finds every required evidence label.
    - `rg -n "PROJECT-LLM-WIKI:START|PROJECT-LLM-WIKI:END|read \\.llm-wiki/index.md first|Current repository files are authoritative|Do not use \\.llm-wiki/ for active task status" .planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md` finds the managed-section rules in the report.
  </acceptance_criteria>
  <done>`peasydeal_be` dry-run evidence is recorded phase-locally and proves the validation did not create `.llm-wiki/` or otherwise dirty the target repo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify package behavior still passes after real-repo dry-run</name>
  <files>.planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md</files>
  <read_first>
    - .planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md
    - skills/project-llm-wiki/scripts/project_wiki.py
    - skills/project-llm-wiki/tests/test_project_wiki_init.py
    - skills/project-llm-wiki/tests/test_project_wiki_package.py
    - .planning/phases/05-agent-instructions-and-real-repo-validation/05-VALIDATION.md
  </read_first>
  <behavior>
    - Targeted init tests still pass after validating the real repo dry-run.
    - Full package tests still pass after validating the real repo dry-run.
    - The rollout report records test evidence used later by the final PASS/FLAG/BLOCK verdict.
  </behavior>
  <action>
    Run:
    ```bash
    PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_init.py"
    PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests
    ```

    Append a `## Package Test Evidence` section to `05-ROLLOUT-REPORT.md` with a table:
    ```markdown
    | Check | Command | Result | Status |
    |---|---|---|---|
    | Targeted init tests | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_init.py"` | `<actual summary>` | PASS/FAIL |
    | Full package suite | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests` | `<actual summary>` | PASS/FAIL |
    ```
  </action>
  <verify>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_init.py"</automated>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests</automated>
    <automated>rg -n "Package Test Evidence|Targeted init tests|Full package suite|PASS|FAIL" .planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_init.py"` exits 0.
    - `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests` exits 0.
    - `rg -n "Package Test Evidence|Targeted init tests|Full package suite" .planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md` finds the test evidence section.
  </acceptance_criteria>
  <done>The rollout report contains both real-repo dry-run evidence and package test evidence for the final rollout verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source repo -> target repo | The helper script is run from this project but with working directory inside `peasydeal_be`; the target repo must remain dry-run only. |
| Dry-run output -> rollout report | Executor copies relevant dry-run evidence into a phase-local report without storing secrets or mutating target files. |
| Target git status -> validation proof | Before/after status output is the evidence that the target repo stayed unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | `peasydeal_be` validation command | mitigate | Run only `init --dry-run` from `/Users/huangchihan/develop/bbj/peasydeal/peasydeal_be`; never run plain `init` or file-changing git commands in the target. |
| T-05-07 | Repudiation | rollout report | mitigate | Record target path, resolved git root, command, exit code, before/after `git status --short`, and `test ! -d .../.llm-wiki` result. |
| T-05-08 | Tampering | cross-repo path selection | mitigate | Use the exact researched target path `/Users/huangchihan/develop/bbj/peasydeal/peasydeal_be`; do not substitute `/Users/huangchihan/develop/bbj/peasydeal_be`. |
| T-05-09 | Information Disclosure | dry-run evidence capture | accept | The report records only dry-run labels and managed Project LLM Wiki section text; it does not copy full target repo files or secrets. |
</threat_model>

<verification>
- `git -C /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be status --short`
- `test ! -d /Users/huangchihan/develop/bbj/peasydeal/peasydeal_be/.llm-wiki`
- `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_init.py"`
- `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests`
- `rg -n "peasydeal_be Dry-Run Evidence|Managed AGENTS.md section:|Conflict status:|Package Test Evidence" .planning/phases/05-agent-instructions-and-real-repo-validation/05-ROLLOUT-REPORT.md`
</verification>

<success_criteria>
- `peasydeal_be` dry-run runs from the actual target repo path and exits 0 or records concrete conflicts.
- The target repo status is captured before and after and remains unchanged.
- The target repo does not gain `.llm-wiki/`.
- The report includes resolved git root, would-create/would-update paths, managed root `AGENTS.md` section, and conflict status.
- Targeted init tests and the full package suite pass after dry-run validation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-instructions-and-real-repo-validation/05-02-SUMMARY.md`.
</output>

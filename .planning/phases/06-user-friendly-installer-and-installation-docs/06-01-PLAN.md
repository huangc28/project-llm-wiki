---
phase: 06-user-friendly-installer-and-installation-docs
plan: 01
type: execute
wave: 1
depends_on:
  - "05-03"
files_modified:
  - install.sh
  - README.md
  - skills/project-llm-wiki/scripts/project_wiki.py
  - skills/project-llm-wiki/tests/test_project_wiki_install.py
  - skills/project-llm-wiki/tests/test_project_wiki_package.py
  - skills/project-llm-wiki/references/command-surface.md
  - skills/project-llm-wiki/references/package-contract.md
autonomous: true
requirements:
  - INSTALL-01
  - INSTALL-02
  - INSTALL-03
  - INSTALL-04
  - INSTALL-05
  - INSTALL-06
  - TEST-08
  - TEST-09
  - TEST-10
must_haves:
  truths:
    - "README primary install command is exactly `curl -fsSL https://raw.githubusercontent.com/huangc28/project-llm-wiki/main/install.sh | bash`."
    - "`install.sh` is a thin bootstrap that clones or updates the package and delegates to `project_wiki.py install`."
    - "`project_wiki.py install` installs the five Codex skills into `${CODEX_HOME:-~/.codex}/skills` by default."
    - "Install never initializes `.llm-wiki/` and never patches root `AGENTS.md`."
    - "Existing real files or directories in the skill target are conflicts."
  artifacts:
    - path: install.sh
      provides: "one-command public bootstrap"
      contains: "project_wiki.py install"
    - path: skills/project-llm-wiki/scripts/project_wiki.py
      provides: "install subcommand and safety behavior"
      contains: "install"
    - path: README.md
      provides: "user-first install and quick start docs"
      contains: "curl -fsSL"
    - path: skills/project-llm-wiki/tests/test_project_wiki_install.py
      provides: "installer safety and boundary tests"
      contains: "test_install"
---

<objective>
Implement the user-friendly Project LLM Wiki installer.

Purpose: make the first-run experience simple enough for users to understand immediately while keeping installer behavior safe, idempotent, and testable.
Output: root `install.sh`, `project_wiki.py install`, focused installer tests, and docs that lead with the one-command install path.
</objective>

<execution_context>
@/Users/huangchihan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangchihan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-user-friendly-installer-and-installation-docs/06-CONTEXT.md
@.planning/phases/06-user-friendly-installer-and-installation-docs/06-RESEARCH.md
@.planning/phases/06-user-friendly-installer-and-installation-docs/06-VALIDATION.md
@README.md
@skills/project-llm-wiki/scripts/project_wiki.py
@skills/project-llm-wiki/tests/test_project_wiki_package.py
@skills/project-llm-wiki/references/command-surface.md
@skills/project-llm-wiki/references/package-contract.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add installer tests first</name>
  <files>skills/project-llm-wiki/tests/test_project_wiki_install.py, skills/project-llm-wiki/tests/test_project_wiki_package.py</files>
  <read_first>
    - skills/project-llm-wiki/tests/test_project_wiki_package.py
    - skills/project-llm-wiki/tests/test_project_wiki_init.py
    - skills/project-llm-wiki/scripts/project_wiki.py
  </read_first>
  <behavior>
    - Tests use temporary directories and never touch the real `~/.codex`.
    - Tests invoke `project_wiki.py install` through subprocess.
    - Tests lock the five installed skill names: `project-llm-wiki`, `project-wiki-init`, `project-wiki-lint`, `project-wiki-query`, `project-wiki-ingest`.
  </behavior>
  <action>
    Create `skills/project-llm-wiki/tests/test_project_wiki_install.py`.

    Add tests for:
    - fresh install creates the five expected symlinks under `--target`.
    - second install exits 0 and reports no unsafe change.
    - `--dry-run` prints planned installs and writes nothing.
    - an existing real directory at one skill path aborts and leaves other paths untouched.
    - stale symlink replacement fails without `--force` and succeeds with `--force`.
    - `--uninstall` removes only symlinks that resolve inside this package and preserves foreign symlinks.
    - install does not create `.llm-wiki/` or modify root `AGENTS.md` in the current repo.

    Extend package/docs tests to assert `project_wiki.py install --help` contains `--target`, `--dry-run`, `--force`, and `--uninstall`.
  </action>
  <verify>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_install.py"</automated>
  </verify>
  <acceptance_criteria>
    - The new installer tests fail before implementation and pass after Task 2.
    - Tests never reference the real user home except through temporary fixtures.
  </acceptance_criteria>
  <done>Installer behavior is specified by focused tests before implementation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement `project_wiki.py install` and root bootstrap</name>
  <files>skills/project-llm-wiki/scripts/project_wiki.py, install.sh</files>
  <read_first>
    - skills/project-llm-wiki/scripts/project_wiki.py
    - skills/project-llm-wiki/tests/test_project_wiki_install.py
    - .planning/phases/06-user-friendly-installer-and-installation-docs/06-CONTEXT.md
    - .planning/phases/06-user-friendly-installer-and-installation-docs/06-RESEARCH.md
  </read_first>
  <behavior>
    - `project-wiki install` defaults to `${CODEX_HOME:-~/.codex}/skills`.
    - The parent Codex home must exist; if not, fail with a clear message telling the user to install or run Codex first.
    - The target skills directory may be created when the Codex home exists.
    - Correct symlinks are no-ops.
    - Existing non-symlink paths are conflicts.
    - Existing symlinks to the wrong location are conflicts unless `--force` is passed.
    - `--uninstall` removes only symlinks resolving inside the package root.
  </behavior>
  <action>
    Add constants for the five skill directory names.

    Add install helpers that:
    - resolve package root from `project_wiki.py` location.
    - verify all five source skill directories exist before any write.
    - plan all target link actions before applying any write.
    - render human-readable output for installed, skipped, replaced, removed, and conflicted entries.

    Add parser command:
    - `project-wiki install`
    - `project-wiki install --target PATH`
    - `project-wiki install --dry-run`
    - `project-wiki install --force`
    - `project-wiki install --uninstall`

    Add root `install.sh` with POSIX shell:
    - `set -eu`
    - default install dir `${PROJECT_LLM_WIKI_HOME:-$HOME/.local/share/project-llm-wiki}`
    - clone `https://github.com/huangc28/project-llm-wiki.git` when the directory is absent
    - `git -C "$dir" pull --ff-only` when the directory exists as a git checkout
    - call `python3 "$dir/skills/project-llm-wiki/scripts/project_wiki.py" install "$@"`
    - print `Restart Codex, then run $project-wiki-init inside a target Git repo.`
  </action>
  <verify>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_install.py"</automated>
    <automated>python3 skills/project-llm-wiki/scripts/project_wiki.py install --help</automated>
  </verify>
  <acceptance_criteria>
    - `python3 skills/project-llm-wiki/scripts/project_wiki.py install --help` exits 0 and documents all install flags.
    - Installer tests pass.
    - Root `install.sh` contains no core filesystem policy beyond clone/update/delegate.
  </acceptance_criteria>
  <done>The package has a safe install subcommand and a thin one-command public bootstrap.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rewrite install docs around the shortest path</name>
  <files>README.md, skills/project-llm-wiki/references/command-surface.md, skills/project-llm-wiki/references/package-contract.md, skills/project-llm-wiki/tests/test_project_wiki_package.py</files>
  <read_first>
    - README.md
    - skills/project-llm-wiki/references/command-surface.md
    - skills/project-llm-wiki/references/package-contract.md
    - skills/project-llm-wiki/tests/test_project_wiki_package.py
  </read_first>
  <behavior>
    - README first install section is understandable without knowing symlinks, Python paths, or Codex internals.
    - Manual symlink and direct Python paths are advanced fallback material only.
    - Package contract no longer says installation is future-only.
  </behavior>
  <action>
    Update README:
    - Status says v1 includes an installer.
    - Install section starts with the one-line curl command.
    - Quick Start says restart Codex, then `$project-wiki-init`.
    - Add advanced/fallback section for direct clone/Python/manual symlink.

    Update `command-surface.md` with `project-wiki install` examples and safety notes.

    Update `package-contract.md` to state Phase 6 owns global Codex skill installation while preserving the target-repo initialization boundary.

    Add package tests that assert README contains:
    - `curl -fsSL https://raw.githubusercontent.com/huangc28/project-llm-wiki/main/install.sh | bash`
    - `Restart Codex`
    - `$project-wiki-init`
  </action>
  <verify>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_package.py"</automated>
    <automated>rg -n "curl -fsSL https://raw.githubusercontent.com/huangc28/project-llm-wiki/main/install.sh \\| bash|Restart Codex|\\$project-wiki-init" README.md</automated>
  </verify>
  <acceptance_criteria>
    - A new user can understand install from README without reading fallback sections.
    - Docs describe install/init as separate write boundaries.
    - Package documentation tests pass.
  </acceptance_criteria>
  <done>Docs lead with the one-command install and move complexity out of the primary path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Run full verification</name>
  <files>all changed files</files>
  <read_first>
    - .planning/phases/06-user-friendly-installer-and-installation-docs/06-VALIDATION.md
  </read_first>
  <behavior>
    - Full package suite passes.
    - Verification proves install and init boundaries remain separate.
  </behavior>
  <action>
    Run the targeted installer tests, package docs tests, then the full package test suite.
  </action>
  <verify>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_install.py"</automated>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_package.py"</automated>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests</automated>
  </verify>
  <acceptance_criteria>
    - All three verification commands exit 0.
    - `git status --short` shows only expected implementation, docs, tests, and Phase 6 planning artifacts.
  </acceptance_criteria>
  <done>Phase 6 installer implementation is verified and ready for summary.</done>
</task>

</tasks>

<verification>
Run:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_install.py"
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests -p "test_project_wiki_package.py"
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s skills/project-llm-wiki/tests
```
</verification>

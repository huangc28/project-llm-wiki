---
phase: 02-init-and-wiki-templates
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - skills/project-llm-wiki/tests/test_project_wiki_init.py
  - skills/project-llm-wiki/tests/test_project_wiki_package.py
  - skills/project-llm-wiki/references/testing.md
autonomous: true
requirements:
  - INIT-01
  - INIT-02
  - INIT-03
  - INIT-04
  - INIT-05
  - INIT-06
  - RAW-01
  - RAW-02
  - RAW-03
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "TEST-01: a clean temporary git repo can run init and `git status --short` shows `.llm-wiki/` files."
    - "TEST-02: rerunning init preserves sentinel-edited wiki files and does not duplicate generated sections."
    - "D-01 through D-05 are covered by tests for subdirectory root resolution, non-git parent refusal, absent `--target`, concise output, and no shared parent wiki creation."
    - "D-06 through D-11 are covered by tests for required skeleton paths, `.gitkeep`, `features/ideas.md`, index links, short template sections, and existing-note preservation."
    - "D-12 through D-16 are covered by tests for README/AGENTS-only repo overview seeding and skipped-source reporting."
    - "D-17 through D-20 are covered by tests for sentinel preservation, missing index link reporting, conflict preflight, and dry-run no side effects."
    - "D-21 through D-25 are covered by tests for raw allow/deny policy text, curated raw guidance, no init secret scanning claim, and the example filename `2026-05-13-api-notes.md`."
  artifacts:
    - path: skills/project-llm-wiki/tests/test_project_wiki_init.py
      provides: "temporary git repo integration tests for init behavior"
      contains: "git status --short"
    - path: skills/project-llm-wiki/references/testing.md
      provides: "Phase 2 validation reference"
      contains: "clean temporary Git repositories"
  key_links:
    - from: skills/project-llm-wiki/tests/test_project_wiki_init.py
      to: skills/project-llm-wiki/scripts/project_wiki.py
      via: "subprocess helper execution"
      pattern: "project_wiki.py"
    - from: skills/project-llm-wiki/tests/test_project_wiki_init.py
      to: "Git status visibility"
      via: "git status --short"
      pattern: "git\", \"status\", \"--short"
---

<objective>
Add the Wave 0 RED validation fixtures that prove Phase 2 behavior end to end before implementation starts.

Purpose: make init safety, skeleton creation, raw policy, seed-source limits, git status visibility, and rerun preservation executable before production code changes. This plan intentionally creates tests that fail until Plans 02-01 and 02-02 implement the behavior; the RED gate succeeds only when the new validation suite is present and not yet green.
Output: integration tests under `skills/project-llm-wiki/tests/` and updated testing reference documentation.
</objective>

<execution_context>
@/Users/huangchihan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangchihan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-init-and-wiki-templates/02-CONTEXT.md
@.planning/phases/02-init-and-wiki-templates/02-RESEARCH.md
@.planning/phases/02-init-and-wiki-templates/02-VALIDATION.md
@.planning/phases/02-init-and-wiki-templates/02-PATTERNS.md
@skills/project-llm-wiki/scripts/project_wiki.py
@skills/project-llm-wiki/tests/test_project_wiki_package.py
@skills/project-llm-wiki/references/testing.md

<interfaces>
Use the existing subprocess test style:
```python
subprocess.run([sys.executable, str(PACKAGE / "scripts" / "project_wiki.py"), *args], cwd=repo, capture_output=True, text=True, check=False)
```

Use temporary repositories:
```python
with tempfile.TemporaryDirectory() as tmp:
    repo = Path(tmp)
    subprocess.run(["git", "init"], cwd=repo, check=True, capture_output=True, text=True)
```

RED gate contract:
```bash
python3 -c 'import subprocess, sys; result = subprocess.run([sys.executable, "-m", "unittest", "discover", "-s", "skills/project-llm-wiki/tests"]); sys.exit(0 if result.returncode != 0 else 1)'
```

That command exits 0 only while the newly added Phase 2 tests are present and still failing because implementation has not run. Plans 02-01 and 02-02 own the GREEN transition.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add clean git repo, subdirectory, dry-run, parent-refusal, and conflict tests</name>
  <files>skills/project-llm-wiki/tests/test_project_wiki_init.py</files>
  <read_first>
    - skills/project-llm-wiki/scripts/project_wiki.py
    - skills/project-llm-wiki/tests/test_project_wiki_package.py
    - .planning/phases/02-init-and-wiki-templates/02-VALIDATION.md
    - .planning/phases/02-init-and-wiki-templates/02-CONTEXT.md
    - .planning/phases/02-init-and-wiki-templates/02-RESEARCH.md
    - .planning/phases/02-init-and-wiki-templates/02-PATTERNS.md
  </read_first>
  <action>
    Create `skills/project-llm-wiki/tests/test_project_wiki_init.py` using only standard-library imports: `subprocess`, `sys`, `tempfile`, `unittest`, and `Path` from `pathlib`.

    Add constants `ROOT = Path(__file__).resolve().parents[3]`, `PACKAGE = ROOT / "skills" / "project-llm-wiki"`, and `HELPER = PACKAGE / "scripts" / "project_wiki.py"`.

    Add helper methods:
    - `run_git(self, repo: Path, *args: str)` that runs `subprocess.run(["git", *args], cwd=repo, capture_output=True, text=True, check=False)`.
    - `init_repo(self, repo: Path)` that runs `git init` and asserts return code 0.
    - `run_helper(self, cwd: Path, *args: str)` that runs `[sys.executable, str(HELPER), *args]` with the supplied cwd.

    Add these tests with exact assertions:
    - `test_init_creates_wiki_at_git_root_from_subdirectory`: create a temp git repo with `README.md` and `AGENTS.md`, create `nested/deeper`, run helper from `nested/deeper` with `init`, assert return code 0, assert `repo/.llm-wiki/README.md`, `repo/.llm-wiki/index.md`, `repo/.llm-wiki/raw/README.md`, `repo/.llm-wiki/raw/curated/README.md`, `repo/.llm-wiki/features/ideas.md`, `repo/.llm-wiki/summaries/repo-overview.md`, `repo/.llm-wiki/architecture/.gitkeep`, `repo/.llm-wiki/domain/.gitkeep`, `repo/.llm-wiki/decisions/.gitkeep`, and `repo/.llm-wiki/operations/.gitkeep` exist; assert `nested/deeper/.llm-wiki` does not exist; assert output contains `Resolved git root:` and `Created paths:`.
    - `test_init_refuses_parent_workspace_with_child_repo_candidate`: create a temp parent with child directory `api`, run `git init` in `api`, run helper from parent with `init`, assert nonzero return code, assert parent `.llm-wiki` does not exist, assert output contains `No git repository found for current directory.`, `Candidate child repositories:`, `api`, and `cd into the intended repo`.
    - `test_dry_run_reports_without_writing`: create a temp git repo with `README.md`, run helper with `init --dry-run`, assert return code 0, assert `.llm-wiki` does not exist, assert output contains `Resolved git root:`, `Would create paths:`, `Sources found: README.md`, and `Skipped sources: AGENTS.md`.
    - `test_conflict_fails_before_partial_writes`: create a temp git repo, create `.llm-wiki` directory and `.llm-wiki/features` as a file, run helper with `init`, assert nonzero return code, assert output contains `.llm-wiki/features: expected directory, found file`, assert `.llm-wiki/README.md` does not exist.

    Do not modify production code in this task. The expected result is RED: the tests exist and fail against the current Phase 1 helper.
  </action>
  <verify>
    <automated>python3 -c 'import subprocess, sys; result = subprocess.run([sys.executable, "-m", "unittest", "discover", "-s", "skills/project-llm-wiki/tests"]); sys.exit(0 if result.returncode != 0 else 1)'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f skills/project-llm-wiki/tests/test_project_wiki_init.py`
    - `rg -n "test_init_creates_wiki_at_git_root_from_subdirectory|test_init_refuses_parent_workspace_with_child_repo_candidate|test_dry_run_reports_without_writing|test_conflict_fails_before_partial_writes" skills/project-llm-wiki/tests/test_project_wiki_init.py` finds all four tests.
    - `rg -n "git\", \"init|git\", \"status\", \"--short|TemporaryDirectory" skills/project-llm-wiki/tests/test_project_wiki_init.py` finds fixture git usage.
    - The RED gate command exits 0 because the new tests fail before implementation.
  </acceptance_criteria>
  <done>RED integration tests exist for clean repo init, subdirectory root targeting, parent workspace refusal, dry-run no side effects, and conflict preflight.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add idempotency, git-status, raw policy, and seed-source tests</name>
  <files>skills/project-llm-wiki/tests/test_project_wiki_init.py, skills/project-llm-wiki/tests/test_project_wiki_package.py, skills/project-llm-wiki/references/testing.md</files>
  <read_first>
    - skills/project-llm-wiki/tests/test_project_wiki_init.py
    - skills/project-llm-wiki/tests/test_project_wiki_package.py
    - skills/project-llm-wiki/references/testing.md
    - skills/project-llm-wiki/scripts/project_wiki.py
    - .planning/phases/02-init-and-wiki-templates/02-VALIDATION.md
    - .planning/phases/02-init-and-wiki-templates/02-CONTEXT.md
    - .planning/phases/02-init-and-wiki-templates/02-RESEARCH.md
  </read_first>
  <action>
    Extend `test_project_wiki_init.py` with these tests:
    - `test_clean_repo_status_shows_llm_wiki_files`: run init in a temp git repo, then run `git status --short`; assert output contains `?? .llm-wiki/` or individual `.llm-wiki/` untracked paths, and assert `.llm-wiki/README.md`, `.llm-wiki/index.md`, `.llm-wiki/raw/README.md`, `.llm-wiki/raw/curated/README.md`, `.llm-wiki/features/ideas.md`, and `.llm-wiki/summaries/repo-overview.md` exist.
    - `test_rerun_preserves_existing_files_and_reports_missing_index_links`: run init, replace `.llm-wiki/index.md` with `# Custom Index\n\nSentinel\n`, replace `.llm-wiki/summaries/repo-overview.md` with `# Custom Overview\n\nSentinel\n`, rerun init, assert both files still contain `Sentinel`, assert output contains `Skipped existing paths:` and `Missing recommended index links:`, and assert output contains `[[features/ideas]]` and `[[summaries/repo-overview]]`.
    - `test_repo_overview_uses_only_readme_and_agents`: create `README.md`, `AGENTS.md`, `package.json`, `pyproject.toml`, `go.mod`, and `Cargo.toml`, run init, assert `.llm-wiki/summaries/repo-overview.md` contains `Sources found: README.md, AGENTS.md`, and assert it does not contain `package.json`, `pyproject.toml`, `go.mod`, or `Cargo.toml`.
    - `test_raw_policy_and_ideas_content`: run init, read `.llm-wiki/raw/README.md`, `.llm-wiki/raw/curated/README.md`, and `.llm-wiki/features/ideas.md`; assert the raw README contains `secrets, credentials, auth tokens, private customer data, full logs, database exports, generated dumps`; assert curated README contains `de-secreted`, `intentionally selected`, `small sources or excerpts`, and `2026-05-13-api-notes.md`; assert ideas page contains `Thought`, `Why it might matter`, `Current leaning`, `Not decided`, `Related links`, and `not a roadmap, backlog, or active task-state store`.

    Update `test_project_wiki_package.py` import whitelist if Plan 02-01 did not already include every standard-library module imported by `project_wiki.py`. The allowed set must contain only modules actually used by the helper, such as `argparse`, `pathlib`, `subprocess`, `sys`, and `textwrap`; add `dataclasses` only if the helper imports it.

    Update `skills/project-llm-wiki/references/testing.md`: replace the Phase 1 import-only wording with Phase 2 validation wording that names `clean temporary Git repositories`, `git status --short`, `init --dry-run`, `idempotency`, `raw policy content`, and the command `python3 -m unittest discover -s skills/project-llm-wiki/tests`.

    Do not modify production code in this task. Preserve the RED gate: the complete Phase 2 validation suite should still fail until Plans 02-01 and 02-02 implement the behavior.
  </action>
  <verify>
    <automated>python3 -c 'import subprocess, sys; result = subprocess.run([sys.executable, "-m", "unittest", "discover", "-s", "skills/project-llm-wiki/tests"]); sys.exit(0 if result.returncode != 0 else 1)'</automated>
    <automated>rg -n "clean temporary Git repositories|git status --short|init --dry-run|idempotency|raw policy content" skills/project-llm-wiki/references/testing.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "test_clean_repo_status_shows_llm_wiki_files|test_rerun_preserves_existing_files_and_reports_missing_index_links|test_repo_overview_uses_only_readme_and_agents|test_raw_policy_and_ideas_content" skills/project-llm-wiki/tests/test_project_wiki_init.py` finds all four tests.
    - `rg -n "package\\.json|pyproject\\.toml|go\\.mod|Cargo\\.toml" skills/project-llm-wiki/tests/test_project_wiki_init.py` finds manifest exclusion assertions.
    - `rg -n "secrets, credentials, auth tokens, private customer data, full logs, database exports, generated dumps|2026-05-13-api-notes.md" skills/project-llm-wiki/tests/test_project_wiki_init.py` finds raw policy assertions.
    - `rg -n "clean temporary Git repositories|git status --short|init --dry-run|idempotency|raw policy content" skills/project-llm-wiki/references/testing.md` finds all five validation phrases.
    - The RED gate command exits 0 because the complete Phase 2 suite is still intentionally RED before implementation.
  </acceptance_criteria>
  <done>RED tests exist for git status visibility, rerun idempotency, raw policy content, seed-source limits, and Phase 2 testing documentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture repos -> helper subprocess | Temporary repos simulate untrusted caller locations and filesystem states. |
| fixture files -> generated wiki | README/AGENTS and manifest fixture files test source selection limits. |
| git status output -> validation claim | Git CLI output proves `.llm-wiki/` visibility instead of assuming it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | idempotency tests | mitigate | Add sentinel-file rerun assertions for `.llm-wiki/index.md` and `.llm-wiki/summaries/repo-overview.md`. |
| T-02-12 | Information disclosure | seed-source tests | mitigate | Add manifest exclusion assertions for `package.json`, `pyproject.toml`, `go.mod`, and `Cargo.toml`. |
| T-02-13 | Tampering | conflict fixture | mitigate | Assert a directory/file conflict returns nonzero before `.llm-wiki/README.md` is created. |
| T-02-14 | Repudiation | git visibility validation | mitigate | Assert `git status --short` shows `.llm-wiki/` as untracked after clean repo init. |
| T-02-15 | Information disclosure | raw policy validation | mitigate | Assert raw templates include the exact unsafe-material denied list and curated-source requirements. |
</threat_model>

<verification>
Run the RED gate command from `<interfaces>`. It must exit 0 because the new Phase 2 validation tests fail against the current unimplemented helper. Confirm the suite covers clean git repo init, subdirectory root targeting, parent workspace refusal, dry-run no side effects, conflict preflight, idempotency, `git status --short`, README/AGENTS-only seeding, raw policy strings, curated raw guidance, and ideas page fields.
</verification>

<success_criteria>
- The Phase 2 package test suite contains failing RED tests for all Phase 2 roadmap requirements and locked decisions through temporary git repo fixtures.
- The RED gate exits 0 by confirming the suite is not yet green before implementation.
- `skills/project-llm-wiki/references/testing.md` documents the Phase 2 validation contract and exact test command.
</success_criteria>

<output>
After completion, create `.planning/phases/02-init-and-wiki-templates/02-03-SUMMARY.md`.
</output>
